# Marble Race Picker — agent instructions

This project keeps its instructions in [`CLAUDE.md`](CLAUDE.md). Read that file; it is the
single source and this one is only a pointer, so the two cannot drift.

Its "Three.js and R3F Work" section routes to Claude Code skills. If your harness has no
equivalent, the rest of that section still applies: it names which parts of this codebase the
guidance covers, and the two React Three Fiber lifecycle traps that have already caused bugs.
